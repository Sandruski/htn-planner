
(:domain multithreading_domain

	// Entry point to call from C++ for the planner, this is consider the main behave function for
	// an AIIndividual.
	(:method (entry_point)

		( branch_decompose_iteration_number
			(and (iteration_number ?index)
			)
			(    (!log ?index)
			)
		)
	)
)