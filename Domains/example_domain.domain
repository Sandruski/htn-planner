
(:domain example_domain

	(:method (eat ?type)

		( branch_in_possession
			(and	(not (item ?x))
					(edible ?x ?y)
					//(speed ?y)
			)
			(	(!take ?x)
			    (!prepare ?y)
			    (!eat ?x)
			)
		)

		( branch_fallback
			(
			)
			(	(!log "Fallback")
			)
		)
	)
)
